# Delegation Chain

子线程可以推动下一步，但必须通过 Delegation Packet。

允许：

```text
Planner → Reviewer → Developer → Reviewer → Synthesizer → Chief of Staff
```

禁止：

```text
子线程无限递归开线程
跳过 Reviewer
跳过 Goal
跳过 Task Card
幕僚长亲自做审核
```
